import { ObjectContainsPipe } from './object-contains.pipe';

describe('ObjectContainsPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectContainsPipe();
    expect(pipe).toBeTruthy();
  });
});
