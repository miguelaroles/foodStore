import { CountCategoryPipe } from './count-category.pipe';

describe('CountCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new CountCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
